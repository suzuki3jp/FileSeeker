[package]
name = "FileSeeker"
description = "A CLI application for analyzing directories and files."
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "*", features = ["derive"] }
