[package]
name = "FileSeeker"
description = "A CLI application for analyzing directories and files."
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.11", features = ["derive"] }

[dev-dependencies]
tempfile = "3.10.1"
